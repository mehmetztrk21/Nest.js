Docker
------------------------
🔹 1. İmaj (Image) İşlemleri

docker pull <image>       # Docker Hub’dan imaj indir
docker images             # Yereldeki imajları listele
docker rmi <image_id>     # İmaj sil
docker build -t name .    # Dockerfile’dan imaj oluştur

🔹 2. Container İşlemleri

docker run <image>                 # Yeni container başlat
docker run -it <image> bash        # Etkileşimli başlat (terminal açar)
docker ps                          # Çalışan container’ları listele
docker ps -a                       # Tüm container’ları listele (durdurulmuş dahil)
docker stop <container_id>         # Container durdur
docker start <container_id>        # Durdurulmuş container’ı başlat
docker restart <container_id>      # Container yeniden başlat
docker rm <container_id>           # Container sil

🔹 3. Log ve Bağlantı İşlemleri

docker logs <container_id>         # Container loglarını görüntüle
docker exec -it <container_id> sh  # Çalışan container içine gir


🔹 4. Sistem Yönetimi

docker version      # Docker versiyonunu öğren
docker info         # Sistem bilgilerini göster
docker system df    # Disk kullanımını göster
docker system prune # Kullanılmayan imaj, container, volume temizle

---------------------- ---------------------- -------------- ---------------------- ---------------------- --------------

Middleware vs Guard

Middleware	
Temel Amaç :	İsteği işlemek, değiştirmek, günlüklemek (logging) veya Kimlik Doğrulama (Authentication) gibi genel işlemler için kullanılır.	
Odak Noktası : Uygulamanın bütününde veya belirli rota gruplarında genel istek öncesi görevler.
Route Bilgisi : Genellikle bilmez. Middleware, route işleyicisine ulaşmadan önce çalışır ve hangi Controller veya handler'ın çağrılacağını bilmez. Bu yüzden kararları yalnızca istek/cevap nesnesine dayanır.
Karar Mekanizması : next() çağırarak isteğin bir sonraki adıma (Guard'a veya Controller'a) ilerlemesini sağlar veya bir yanıt göndererek döngüyü sonlandırır.

Guard
Temel Amaç : Yetkilendirme (Authorization) ve erişim kontrolü yapmak için kullanılır. Bir kullanıcının belirli bir rotaya/kaynağa erişim izni olup olmadığını belirler.
Odak Noktası : Belirli bir rota işleyicisine (route handler) erişimi kısıtlamak.
Route Bilgisi : Bilir. ExecutionContext sayesinde çağrılmakta olan Controller ve method hakkında bilgiye (metadata) erişebilir.
Karar Mekanizması : boolean (true veya false) değeri döndürür. true ise erişim izni verir, false ise erişimi engeller (genellikle bir HTTP istisnası fırlatır).

Middleware her zaman Guard'lardan önce çalışır.

Çalışma Önceliği

1. Middleware: İlk olarak çalışır. Express'teki geleneksel middleware'ler gibi davranır.

2. Guard: Middleware'den sonra çalışır. Rotaya erişimi etkinleştirmeden önce yetkilendirme kontrolünü yapar.

3. Interceptor (önce): Guard'dan sonra, Controller'dan önce çalışır.

4. Pipe: Controller metodu çağrılmadan hemen önce çalışır.

5. Controller Metodu: Çalıştırılır.




